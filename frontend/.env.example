# Points the frontend at the FastAPI backend. Swap this to the real Azure URL after
# Phase 12 — no code changes needed, everything reads from import.meta.env.VITE_API_BASE_URL.
VITE_API_BASE_URL=http://localhost:8001
